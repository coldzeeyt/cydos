.pio/
.pioenvs/
.piolibdeps/
.vscode/
.clang_complete
.gcc-flags.json
/CydOs.bin

# Generated by scripts/generate_community.py when building the Community
# Edition. src/community_registration.inc itself stays tracked (it's the
# committed no-op stub the normal build uses) - the generator overwrites
# it locally; `git checkout -- src/community_registration.inc` reverts it.
/src/apps/community/
